type Query {
  """Get the installed and latest Kedro-Viz versions"""
  version: Version!
}

"""Installed and latest Kedro-Viz versions"""
type Version {
  installed: String!
  isOutdated: Boolean!
  latest: String!
}
