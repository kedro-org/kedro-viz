"""Read raw project state from disk for the inspection adapter.

Modules:

* ``snapshot_source``: load and filter a project's inspection snapshot and raw config.
* ``enrichment``: load file-backed node extras and layer overrides.
"""
