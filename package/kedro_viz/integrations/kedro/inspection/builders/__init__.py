"""Pure construction logic that turns inspection inputs into API response models.

Modules:

* ``graph_builder``: build the main graph response for one pipeline.
* ``run_status_builder``: build run-status responses from recorded pipeline events.
* ``layers``: extract and sort dataset layers.
* ``modular_pipelines``: modular pipeline groups, tree, and boundary connections.
"""
